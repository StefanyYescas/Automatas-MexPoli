programa {
  inicio {
    16d x;
  }

  >> x;

  x = (x^2 + 2x + 1) * (x + 3);

  if (x > 100) {
    reducir(x);
  } elif (x > 50) {
    ordenar(x);
  } else {
    expandir(x);
  }

  while (x < 500) {
    x = x + (3x + 1);
  }

  << x;
}
